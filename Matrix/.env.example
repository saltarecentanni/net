# TIESSE Matrix Network - Environment Configuration
# Copy this file to .env and customize values
# NEVER commit .env to version control!

# =============================================================================
# NODE.JS SERVER CONFIGURATION
# =============================================================================

# Server port
PORT=3000

# Authentication for Node.js backend (plain text password)
AUTH_USERNAME=tiesse
AUTH_PASSWORD=tiesseadm

# Session timeout in milliseconds (default: 8 hours = 28800000)
SESSION_TIMEOUT=28800000

# Debug mode (set to 'true' to enable console logging)
DEBUG_MODE=false

# Data file path
DATA_FILE=data/network_manager.json

# =============================================================================
# PHP CONFIGURATION (used by config.php)
# =============================================================================

# PHP uses password HASH, not plain text!
# Generate hash with: php -r "echo password_hash('yourpassword', PASSWORD_DEFAULT);"
AUTH_PASSWORD_HASH=$2y$10$e1nfIfvV2sZag1oARGD89.bG9emt6QxSQyHoreh9Ep5cFrFpgXlpm

# PHP session timeout in SECONDS (default: 8 hours = 28800)
# Note: Node.js uses milliseconds, PHP uses seconds
