# TIESSE Matrix Network - Environment Configuration
# Version: 3.6.034 (UI & Documentation Update)
# Copy this file to .env and customize values
# NEVER commit .env to version control!
#
# NOTE: This application is designed for INTERNAL/INTRANET use only.

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Server port
PORT=3000

# Host binding (use 127.0.0.1 for local-only access, 0.0.0.0 for network access)
HOST=0.0.0.0

# =============================================================================
# AUTHENTICATION (SHARED BY NODE.JS AND PHP)
# =============================================================================

# Username for admin access
AUTH_USERNAME=tiesse

# IMPORTANT: Both Node.js and PHP now use bcrypt password HASH (not plain text!)
# Generate hash with either:
#   node -e "require('bcrypt').hash('yourpassword', 10).then(console.log)"
#   php -r "echo password_hash('yourpassword', PASSWORD_DEFAULT);"
# Default hash below is for password 'tiesse' - CHANGE IN PRODUCTION!
AUTH_PASSWORD_HASH=$2y$10$e1nfIfvV2sZag1oARGD89.bG9emt6QxSQyHoreh9Ep5cFrFpgXlpm

# Session timeout in milliseconds (default: 8 hours = 28800000)
SESSION_TIMEOUT=28800000

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================

# CORS allowed origins (comma-separated list)
# For internal use, you can use CORS_ALLOW_ALL=true
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Allow all CORS origins (safe for internal/intranet use)
CORS_ALLOW_ALL=true

# =============================================================================
# OTHER SETTINGS
# =============================================================================

# Debug mode (set to 'true' to enable console logging)
DEBUG_MODE=false

# Data file path
DATA_FILE=data/network_manager.json
